name: Smoke tests

on: [push]

jobs:
  test-all-together-with-latest-versions:
    runs-on: ubuntu-latest

    steps:
      # - uses: actions/checkout@v2
      # - run: |
      #     date > generated.txt
      #     git config user.name nfedyashev
      #     git config user.email nfedyashev@gmail.com
      - name: Prepare java
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Install all the tools together
        uses: DeLaGuardo/setup-clojure@master
        with:
          tools-deps: latest
          lein: latest

      - name: Execute dummy clojure code
        run: lein run -m luminusdiff.core
